let speed = prompt("Enter Speed Of The Car!")

const velocity = 70;

function detector() {
    if (speed == velocity) {
        console.log(OK);
        window.alert(OK);
    }
    else if (speed > velocity){
        
    }


}